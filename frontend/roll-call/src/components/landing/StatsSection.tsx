export function StatsSection() {
  return (
    <section className="wrap reveal">
      <div className="stats">
        <div className="glass-muted stat-pill">3 roles, one ledger</div>
        <div className="glass-muted stat-pill">Full submission history, every edit</div>
        <div className="glass-muted stat-pill">Deadlines enforced automatically</div>
      </div>
    </section>
  );
}
